[Unit]
Description=Dashboard localhost container monitoring
Requires=docker.service
After=docker.service network-online.target
Wants=network-online.target
ConditionPathExists={{ dashboard_monitoring_compose_path }}

[Service]
Type=oneshot
RemainAfterExit=true
WorkingDirectory={{ dashboard_monitoring_directory }}
ExecStart=/usr/bin/docker compose -f {{ dashboard_monitoring_compose_path }} up -d --remove-orphans --wait --wait-timeout 120
ExecReload=/usr/bin/docker compose -f {{ dashboard_monitoring_compose_path }} up -d --remove-orphans --wait --wait-timeout 120
ExecStop=/usr/bin/docker compose -f {{ dashboard_monitoring_compose_path }} stop --timeout 30
TimeoutStartSec=180
TimeoutStopSec=60

[Install]
WantedBy=multi-user.target
