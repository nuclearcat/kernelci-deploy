version: "3.9"

services:
  uptime-kuma:
    image: {{ dashboard_uptime_kuma_image }}
    container_name: uptime-kuma
    restart: always
    ports:
      - "127.0.0.1:{{ dashboard_uptime_kuma_port }}:3001"
    volumes:
      - ./uptime-kuma:/app/data
