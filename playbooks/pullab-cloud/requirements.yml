---
# ansible-galaxy collection install -r requirements.yml
#
# Only aws-infra.yml needs these; site.yml runs on ansible.builtin alone.
collections:
  - name: amazon.aws
    version: ">=6.0.0"
  - name: community.aws
    version: ">=6.0.0"
