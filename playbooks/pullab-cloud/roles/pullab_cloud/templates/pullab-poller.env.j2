# Managed by Ansible - playbooks/pullab-cloud
#
# UNIFIED_TOKEN is the shared fallback for both KERNELCI_API_TOKEN and
# KCIDB_JWT; the config files leave api_token/kcidb_jwt null on purpose.
UNIFIED_TOKEN={{ pullab_unified_token }}
PYTHONPATH=src
